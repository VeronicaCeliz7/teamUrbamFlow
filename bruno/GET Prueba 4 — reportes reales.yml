info:
  name: GET Prueba 4 — reportes reales
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:3001/api/reportes?municipio=villa-maria&estado=pendiente&sinAsignar=true
  params:
    - name: municipio
      value: villa-maria
      type: query
    - name: estado
      value: pendiente
      type: query
    - name: sinAsignar
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
