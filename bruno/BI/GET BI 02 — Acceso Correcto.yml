info:
  name: GET BI 02 — Acceso Correcto
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3001/api/bi/health
  headers:
    - name: x-api-key
      value: urbanflow_bi_read_2026
      enabled: true
    - name: x-scope
      value: urbanflow.bi.read
      enabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5