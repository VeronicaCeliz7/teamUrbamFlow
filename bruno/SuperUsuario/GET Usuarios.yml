info:
  name: GET Usuarios
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3001/api/super/usuarios
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
