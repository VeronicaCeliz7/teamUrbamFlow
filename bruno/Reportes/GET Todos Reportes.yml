info:
  name: GET Todos Reportes
  type: http
  seq: 2

http:
  method: GET
  url: ttp://localhost:3001/api/reportes
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
