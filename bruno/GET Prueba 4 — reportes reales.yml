info:
  name: GET Prueba 4 — reportes reales
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:3001/api/super/reportes
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
