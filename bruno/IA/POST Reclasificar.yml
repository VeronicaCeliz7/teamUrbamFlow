info:
  name: POST Reclasificar
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:3001/api/ia/reclasificar
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "limite": 50
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5