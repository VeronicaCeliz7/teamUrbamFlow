info:
  name: GET Pronostico
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3001/api/clima/pronostico
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5