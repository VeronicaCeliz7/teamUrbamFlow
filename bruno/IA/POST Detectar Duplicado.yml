info:
  name: POST Detectar Duplicado
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3001/api/ai/detect-duplicate
  body:
    type: json
    data: |-
      {
        "texto": "Bache enorme frente al hospital"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
