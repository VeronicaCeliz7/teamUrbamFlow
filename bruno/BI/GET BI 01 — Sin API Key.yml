info:
  name: GET BI 01 — Sin API Key
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3001/api/bi/health

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5