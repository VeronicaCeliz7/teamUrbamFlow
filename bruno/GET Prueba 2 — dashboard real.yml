info:
  name: GET Prueba 2 — dashboard real
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:3001/api/super/dashboard
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
