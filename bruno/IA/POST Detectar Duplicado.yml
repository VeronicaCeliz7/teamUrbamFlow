info:
  name: POST Detectar Duplicado
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3001/api/ai/detect-duplicate
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
