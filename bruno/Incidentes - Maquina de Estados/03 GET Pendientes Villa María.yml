info:
  name: 03 GET Pendientes Villa Maria
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3001/api/reportes?municipio=villa-maria&estado=pendiente
  headers:
    - name: Authorization
      value: Bearer TU_TOKEN_REAL
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5