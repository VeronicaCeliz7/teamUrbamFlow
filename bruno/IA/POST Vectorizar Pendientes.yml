info:
  name: POST Vectorizar Pendientes
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3001/api/ia/vectorizar-pendientes
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "limite": 50
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
