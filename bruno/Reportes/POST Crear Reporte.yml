info:
  name: POST Crear Reporte
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3001/api/reportes
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
