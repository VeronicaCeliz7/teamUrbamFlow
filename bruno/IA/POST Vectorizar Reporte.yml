info:
  name: POST Vectorizar Reporte
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3001/api/ia/vectorizar
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "reporteId": "PEGAR_ID_DE_REPORTE"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5