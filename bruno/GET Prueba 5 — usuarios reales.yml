info:
  name: GET Prueba 5 — usuarios reales
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:3001/api/super/usuarios
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
