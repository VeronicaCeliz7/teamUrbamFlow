info:
  name: GET Prueba 3 — clientes reales
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:3000/api/super/clientes
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
