info:
  name: GET Mis Reportes
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3001/api/reportes/ID_DEL_REPORTE
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
